// app/tickets/static/tickets.js
window.mountTicketsPanel = function mountTicketsPanel(selector) {
  // Optional: enhance table links to use your SPA loader, if needed.
  // Currently your SPA loader already intercepts sidebar clicks; detail links
  // go to full server-rendered pages, which your loader can fetch too.
  // Keep as a safe no-op for now.
  // console.log("Tickets panel mounted", selector);
};
